# Dungeons and Dragons

Some words about what the sub-domain does.

## Desired Structure

Excluding standard README.md, server and tooling files.

```text
.
├── campaigns
  ├── index.html (campaigns home)
  └── thieves-of-capitol
    ├── index.html (campaign home)
    └── * (supporting files)
├── index.html (d&d home)
├── posts (this is for random writing)
  └── year
     └── month
       └──*.html
├── rules
  ├── index.html (current rules)
  └── version-### (past versions)
       └──index.html
├── script
  └── *.js
├── settings
  ├── index.html (settings home)
  ├── the-known-world (home setting)
  └── faerun (grey box)
└── style
  └── *.js
```
